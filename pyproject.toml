[project]
name = "diabetic-retinopathy-cnn"
version = "0.1.0"
description = "Simple CNN for diabetic retinopathy classification"
requires-python = ">=3.13"
dependencies = [
  "torch",
  "torchvision",
  "scikit-learn",
  "matplotlib",
  "tqdm",
]
